<!doctype html>

<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title><%= typeof title !== 'undefined' ? title : 'OpenTok embed appointment demo' %></title>
    <% for (var a of assets.styles) { %>
    <link rel="stylesheet" type="text/css" href="/css/<%= a %>">
    <% } %>
</head>

<body>
  <div class="container header">
    <h1>OpenTok Embed Appointment Demo</h1>
    <% if (user) { %>
      <nav role="main">
        <ul class="row">
          <li><strong><%= user.name || user.username %></strong> (<%= user.role %>)</li>
          <li><a href="/dashboard">Dashboard</a></li>
          <li><a href="/user/logout">Logout</a></li>
        </ul>
      </nav>
    <% } %>
  </div>
  <div class="container">
